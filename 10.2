#include<string.h>

int main() {
    char email[50];
    printf("enter your email:");
    scanf("%s" , email);
   char *d = strchr(email , '@'); //strings after @
   if(d!= NULL){
       d++;
       printf("%s" , d);
   }
   else{
       printf("invalid");
   }

    return 0;
}
